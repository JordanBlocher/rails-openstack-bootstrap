<table class="table table-bordered">
  <tr>
    <th>Address</th>
    <th>Hostname</th>
  </tr>
  <% @hash.each do |key, value| %>
  <tr>
    <td><%= key["address"] %></td>
    <td><%= key["hostname"] %></td>
  </tr>
<% end %>

</table>

<br />

